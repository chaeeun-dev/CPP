

==========================================================================
fileName ::, 2024-07-27 토요일 오후 2:11:20
==========================================================================


#include <iostream>
#include "Array.h"
#include "save.h"

int main()
{
	Array arr(100);

	arr.push_back(1);
	arr.push_back(2);
	arr.push_back(3);
	arr.push_back(4);

	arr[1] = 2;

	save("cpp.cpp");
}


==========================================================================================
// 배열 vs 동적 배열 vs 연결리스트
// 언제 사용해야 유리할까?
// 
// 배열은 초기부터 사이즈를 확신할 수 있고, 할당된 범위 내에서만 사용할 때
// e.g. 맵의 정보에서 크기가 100X100으로 고정
//		눈 코 입 외형 커스터마이징 할 때 저장하는 배열
//		인벤토리
//
// 동적 배열 vs 연결 리스트
// 대부분의 경우 동적 배열이 빠름 - 캐싱으로 다음 데이터 빠르게 접근 가능
// -> 동적 배열로 가는 경우가 더 많음
// 
// 동적 배열 - vector / 연결 리스트 - list
// 
// 연결리스트가 더 좋을 때?
// e.g. [일감][일감][일감]<->[일감][일감][일감][일감]
// 대기열인데 중간에 어떤 게 빼야할 때 
// 실제로 거의 사용하지는 않음! 앞으로 vector를 많이 사용할 것
